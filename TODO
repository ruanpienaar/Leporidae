 ☐ Provide a cast/call option to each {amqp,...} item ?
 ☐ How quickly should leporidae reconnect a disconnected / consumer/producer ?
 ☐ Create common parts for connections, queues and exchanges
         % {connections,[
        %     {connection_1,
        %         {type, network},
        %         {username, <<"root">>},
        %         {passwd, <<"root">>},
        %         {vhost, <<"/">>},
        %         {host, "localhost"},
        %         {port, 5672}
        %     }
        % ]},
        % TODO: same for queues and connections
        %{queues,}
        %{exchanges,}

☐ consumers - multiple consumers for 1 config entry.
☐ consumers - noack when fails.
☐ consumers - cancel a consumer
☐ consumers - ACK and NACK details from sys.config

sys.config:
 ☐ rename producers to publishers
 ☐ create producer id/name, so that code call reference a specific item